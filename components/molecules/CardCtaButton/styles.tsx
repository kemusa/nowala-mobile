import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 25,
    minWidth: 65,
    backgroundColor: '#CCE3FF',
    borderRadius: 8,
    paddingLeft: 5,
    paddingRight: 5,
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
  },
  textConfig: { fontSize: 14, color: '#0057FF' },
});

export default styles;
