import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textInput: {
    height: 40,
    width: '100%',
    backgroundColor: '#fff',
    fontSize: 16,
    fontWeight: '400',
  },
  label: { marginLeft: 5, marginBottom: 3 },
});

export default styles;
