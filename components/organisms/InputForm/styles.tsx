import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputContainer: { marginBottom: 10, backgroundColor: 'blue' },
  errorText: { marginBottom: 6 },
});

export default styles;
