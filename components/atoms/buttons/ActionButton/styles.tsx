import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: { borderRadius: 8, height: 42 },
  text: { fontSize: 20, fontWeight: 'bold', lineHeight: 22, letterSpacing: 0 },
});

export default styles;
