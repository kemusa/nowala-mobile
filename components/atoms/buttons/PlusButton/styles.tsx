import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    height: 40,
    width: 40,
    backgroundColor: '#CBFFE1',
    borderRadius: 20,
    borderWidth: 2,
    borderColor: '#CBFFE1',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
