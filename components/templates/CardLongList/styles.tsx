import { StyleSheet } from 'react-native';
import colors from '../../../theme/colors';
const { NW_GREY_2 } = colors;

const styles = StyleSheet.create({
  border: {
    borderBottomColor: NW_GREY_2,
    width: '100%',
    borderBottomWidth: 1,
  },
});

export default styles;
