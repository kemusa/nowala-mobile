import { StyleSheet } from 'react-native';
// import colors from '../../../../../theme/colors';

// const { WHITE } = colors;
const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: 5,
  },
  col2: { alignItems: 'flex-end' },
});

export default styles;
